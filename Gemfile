# Jekyll para GitHub Pages
source "https://rubygems.org"

# GitHub Pages usa automáticamente esta gema para compatibilidad
gem "github-pages", group: :jekyll_plugins

# Si quieres probar localmente, descomenta y ejecuta: bundle install
# platform :ruby do
#   gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]
# end
